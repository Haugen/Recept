$(document).ready(function() {

  // Write out a few rows from the start.

  // Add a new row to the table.

  // Make the table sortable by jQuery UI.

  // The markup for a new row.
  function addNewRow(rowNr) {

  }
});

// Delete a row.
